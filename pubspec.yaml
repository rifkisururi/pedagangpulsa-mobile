name: pedagangpulsa_mobile
description: A Flutter application for pedagang pulsa
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  url_launcher: ^6.2.0
  screenshot: ^2.1.0
  path_provider: ^2.1.0
  share_plus: ^7.2.0
  intl: ^0.18.0
  image_picker: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
